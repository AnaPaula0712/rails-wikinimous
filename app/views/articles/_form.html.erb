<div class="container p-5 m-3">
  <%= simple_form_for(article) do |f| %>
    <strong><%= f.input :title %></strong>
    <strong><%= f.input :content %></strong>
    <%= f.submit class: 'btn btn-success w-100 mt-3'%>
  <% end %>
  <%= link_to 'home', root_path, class: 'btn btn-primary btn-lg btn-block w-100 mt-3' %>
</div>
